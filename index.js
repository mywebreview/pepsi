var mainListDiv = document.getElementById("navBar"),
    mediaButton = document.getElementById("icon");

mediaButton.onclick = function () {
    
    "use strict";
    
    navBar.classList.toggle("show_list");
    icon.classList.toggle("active");
};